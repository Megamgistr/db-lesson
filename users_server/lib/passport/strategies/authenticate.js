module.exports = async function(strategyName, email, displayName, done) {
	/*
	Найти по email
	Если нет, создать
	создать сессию
	*/


	done(null, false, `function for ${strategyName} doesn't work`)
}